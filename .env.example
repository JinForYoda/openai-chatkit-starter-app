# API keys are now configured in the browser via the Settings panel
# and stored in localStorage. No environment variables required!

# Optional: Custom ChatKit API base URL (default: https://api.openai.com)
# CHATKIT_API_BASE=https://api.openai.com
